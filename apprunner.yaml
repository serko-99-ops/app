version: 1.0
runtime: python3 
build:
  commands:
    build:        
      - pip install -r requirements.txt
run:
  command: python application-f.py
  env:
   - name : NAME_FIRST
     value: "Ser"
   - name : NAME_LAST
     value: "Serko"
